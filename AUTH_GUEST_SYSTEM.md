# 🔐 Система авторизации для гостей - Документация

## ✅ Что было реализовано

Создана гибкая система авторизации, которая позволяет гостям свободно просматривать сайт, но требует авторизации для выполнения действий.

---

## 🎯 Основные функции

### 1. **Свободный доступ для гостей**
- ✅ Просмотр всех кейсов (включая "used" кейсы)
- ✅ Просмотр содержимого кейсов и шансов drop
- ✅ Просмотр условий для открытия
- ✅ Просмотр публичных профилей игроков
- ✅ Просмотр живой ленты дропов
- ✅ Клик и просмотр любых кейсов (нет блокировки "USED TODAY/MONTH")
- ✅ Баланс не отображается (блок с балансом скрыт)

### 2. **Требуется авторизация для:**
- 🔒 Открытия кейсов
- 🔒 Получения (claim) предметов
- 🔒 Пополнения баланса
- 🔒 Доступа к личному инвентарю
- 🔒 Просмотра баланса

---

## 📋 Список изменений

### Измененные файлы:

1. **`/src/app/contexts/LanguageContext.tsx`**
   - Добавлены переводы для модального окна авторизации
   - Ключи: `login.title`, `login.subtitle`, `login.username`, `login.password`, и т.д.
   - Поддержка 3 языков: EN, RU, LV

2. **`/src/app/components/LoginModal.tsx`** (новый)
   - Модальное окно авторизации с красивым дизайном
   - Градиентный заголовок с иконкой ShieldCheck
   - Поля для username и password
   - Показ/скрытие пароля
   - Список из 5 преимуществ авторизации
   - Анимации и индикаторы загрузки

3. **`/src/app/ClientApp.tsx`**
   - Добавлено состояние `isAuthenticated` с сохранением в localStorage
   - Убрано принудительное открытие модалки при загрузке
   - Модальное окно можно закрыть (гость может просматривать сайт)
   - Добавлен компонент Toaster для уведомлений
   - Передача `isAuthenticated` в дочерние компоненты

4. **`/src/app/components/CaseOpenPage.tsx`**
   - Добавлен проп `isAuthenticated`
   - Добавлен проп `onRequestLogin`
   - Условное отображение кнопок:
     - **Гость**: кнопка "SIGN IN" с иконкой Lock
     - **Авторизован**: кнопка "OPEN CASE" или "ADD $ TO OPEN"

5. **`/src/app/components/TopBar.tsx`**
   - Блок баланса скрыт для гостей
   - Баланс виден только авторизованным пользователям

6. **`/src/app/components/CasesPage.tsx`**
   - Добавлен проп `isAuthenticated`
   - Для гостей `deposited` всегда = 0
   - Overlay "USED TODAY/MONTH" показывается только для авторизованных
   - На "USED" кейсах добавлена плашка с депозитом (X/Y€) для авторизованных
   - Гости могут кликать на все кейсы без ограничений

---

## 🎨 UX/UI Детали

### Модальное окно авторизации:
- Темный фон с размытием (backdrop-blur)
- Градиентный заголовок (бордовый #7c2d3a)
- Иконка ShieldCheck для доверия
- Анимации появления
- Может быть закрыто гостем (не блокирует просмотр)

### Кнопки на странице кейса:
- **Для гостя**: бордовая рамка, иконка замка, текст "SIGN IN"
- **Для пользователя**: зеленая/красная рамка в зависимости от баланса

### Toast уведомления:
- Темная тема (#1d1d22)
- Появляются при попытке действий без авторизации
- Текст: "Please sign in to {action}" (переведено на 3 языка)

---

## 🌐 Переводы

### Английский (EN):
```
login.title: "AUTHORIZATION REQUIRED"
login.subtitle: "Sign in to unlock all features"
login.signIn: "SIGN IN"
login.benefit1: "Open cases and win amazing prizes"
...
```

### Русский (RU):
```
login.title: "ТРЕБУЕТСЯ АВТОРИЗАЦИЯ"
login.subtitle: "Войдите, чтобы разблокировать все функции"
login.signIn: "ВОЙТИ"
login.benefit1: "Открывать кейсы и выигрывать призы"
...
```

### Латышский (LV):
```
login.title: "NEPIECIEŠAMA AUTORIZĀCIJA"
login.subtitle: "Piesakieties, lai atbloķētu visas funkcijas"
login.signIn: "PIETEIKTIES"
login.benefit1: "Atvērt kastes un laimēt balvas"
...
```

---

## 💡 Как это работает

1. **При загрузке сайта:**
   - Проверяется localStorage для состояния авторизации
   - Если не авторизован - гость может свободно просматривать
   - Модальное окно НЕ появляется автоматически

2. **Когда гость пытается открыть кейс:**
   - Видит кнопку "SIGN IN" вместо "OPEN CASE"
   - При клике открывается модальное окно авторизации
   - Показывается toast с текстом "Please sign in to open cases"

3. **После авторизации:**
   - Состояние сохраняется в localStorage
   - Модальное окно закрывается
   - Показывается toast "Login successful!"
   - Обновляется баланс с сервера
   - Кнопки меняются на активные

4. **Выход (если реализован):**
   - localStorage.removeItem('isAuthenticated')
   - Состояние сбрасывается
   - Пользователь снова становится гостем

---

## 🔧 Технические детали

### Состояние авторизации:
```typescript
const [isAuthenticated, setIsAuthenticated] = useState(() => {
  return localStorage.getItem('isAuthenticated') === 'true';
});
```

### Проверка при действии:
```typescript
if (!isAuthenticated) {
  setLoginModalOpen(true);
  toast.warning(t('login.needAuth').replace('{action}', t('login.actionOpen')));
  return;
}
```

### API запрос авторизации:
```typescript
const response = await fetch('http://localhost:3000/api/auth/login', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ username, password }),
});
```

---

## 🎯 Преимущества этого подхода

1. **Лучший UX**: Гость может изучить сайт перед регистрацией
2. **Прозрачность**: Видны все кейсы, их содержимое и шансы
3. **Мотивация**: Видя, что можно выиграть, гость захочет авторизоваться
4. **Гибкость**: Легко добавить другие защищенные действия
5. **Безопасность**: Критичные действия требуют авторизации

---

## 🆕 Последние обновления

### **Версия 1.2 - Улучшения для гостевого режима:**

#### 1. **Плашка депозита на USED кейсах**
   - На кейсах с overlay "USED TODAY/MONTH" теперь отображается плашка с депозитом
   - Формат: круговой прогресс + "X/Y€"
   - Показывается только для авторизованных пользователей
   - Помогает понять, сколько было внесено на этот кейс

#### 2. **Скрытие баланса для гостей**
   - Блок баланса в TopBar теперь полностью скрыт для гостей
   - Гости не видят баланс кошелька
   - Кнопка "SIGN IN" находится на видном месте
   - После авторизации баланс появляется автоматически

#### 3. **Полный доступ к просмотру**
   - Гости могут видеть все кейсы без исключений
   - Нет блокировок "USED TODAY/MONTH" для гостей
   - Все кейсы показывают "0/required€" для гостей
   - Клик на любой кейс показывает модальное окно авторизации

---

## 📁 Измененные/созданные файлы:

1. `/src/app/contexts/LanguageContext.tsx` - 🔴 изменен
2. `/src/app/components/LoginModal.tsx` - 🟢 новый
3. `/src/app/ClientApp.tsx` - 🔴 изменен
4. `/src/app/components/CaseOpenPage.tsx` - 🔴 изменен
5. `/src/app/components/TopBar.tsx` - 🔴 изменен
6. `/src/app/components/CasesPage.tsx` - 🔴 изменен
7. `/AUTH_GUEST_SYSTEM.md` - 🟢 этот файл

---

**Готово!** Теперь гости могут свободно изучать сайт, а для действий требуется авторизация! 🎉