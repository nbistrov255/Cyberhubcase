#!/bin/bash

# Скрипт для очистки кэша Vite и перезапуска проекта
# Использование: bash clear-cache.sh

echo "🧹 Очистка кэша Vite..."

# Остановить процесс (если запущен через этот скрипт)
# pkill -f "vite" 2>/dev/null || true

# Удалить кэш Vite
if [ -d "node_modules/.vite" ]; then
  rm -rf node_modules/.vite
  echo "✅ Кэш Vite удален"
else
  echo "⚠️  Кэш Vite не найден"
fi

# Удалить папку dist
if [ -d "dist" ]; then
  rm -rf dist
  echo "✅ Папка dist удалена"
else
  echo "⚠️  Папка dist не найдена"
fi

echo ""
echo "✨ Кэш очищен!"
echo ""
echo "Теперь запустите сервер:"
echo "  npm run dev"
echo ""
echo "И обновите браузер: Ctrl+Shift+R"
