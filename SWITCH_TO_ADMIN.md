# Как запустить Админ-Панель

Приложение теперь разделено на два независимых интерфейса:

## 🎮 **Клиентское приложение (по умолчанию)**
Основное приложение для пользователей - открытие кейсов, инвентарь, профили.

**Файл:** `/src/app/App.tsx`

```tsx
import ClientApp from './ClientApp';

export default function App() {
  return <ClientApp />;
}
```

---

## ⚙️ **Админ-Панель**
Панель управления для персонала - управление кейсами, пользователями, заявками, логами.

### Как переключиться на админ-панель:

**Замените содержимое `/src/app/App.tsx` на:**

```tsx
import AdminApp from './admin/AdminApp';
import { AdminLanguageProvider } from './admin/contexts/AdminLanguageContext';

export default function App() {
  return (
    <AdminLanguageProvider>
      <AdminApp />
    </AdminLanguageProvider>
  );
}
```

Или проще:

```tsx
import AdminAppEntry from './AdminAppEntry';

export default function App() {
  return <AdminAppEntry />;
}
```

---

## 🔐 Данные для входа в админ-панель:

- **Username:** `admin`
- **Password:** `admin123`

---

## 🔄 Как вернуться к клиентскому приложению:

**Верните исходное содержимое в `/src/app/App.tsx`:**

```tsx
import ClientApp from './ClientApp';

export default function App() {
  return <ClientApp />;
}
```

---

## 📝 Примечания:

- Оба приложения полностью независимы
- У них разные системы локализации (клиент и админ)
- Разные стили и компоненты
- В реальном проекте это будут два отдельных домена/поддомена
