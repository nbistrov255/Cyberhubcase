# ⚡ QUICK FIX - Auth Issue

## 🎯 ПРОБЛЕМА

После перезагрузки пользователя выкидывает на страницу логина.

---

## 🔍 ДИАГНОСТИКА (1 команда)

```bash
ssh root@91.107.120.48 'grep -q "ПРОВЕРКА expires_at" /root/cyberhub-backend/src/index.ts && echo "✅ Backend деплоен" || echo "❌ Backend НЕ деплоен - нужен деплой!"'
```

**Если видите `❌ Backend НЕ деплоен`** → переходим к решению ниже.

---

## ✅ РЕШЕНИЕ (2 команды)

### 1️⃣ Деплой backend:
```bash
scp /backend_reference/index.ts root@91.107.120.48:/root/cyberhub-backend/src/index.ts
```

### 2️⃣ Перезапуск:
```bash
ssh root@91.107.120.48 "cd /root/cyberhub-backend && docker-compose restart cyberhub_api"
```

---

## 🧪 ПРОВЕРКА

После деплоя:

1. Откройте сайт: http://91.107.120.48:3000
2. Войдите в систему
3. Нажмите F5 (перезагрузка)
4. ✅ Должен остаться авторизованным!

---

## 📋 ЧТО ДЕЛАЕТ ДЕПЛОЙ

1. ✅ Добавляет проверку `expires_at` в requireSession
2. ✅ Добавляет обновление `last_seen_at` при каждом запросе
3. ✅ Добавляет endpoint `GET /api/user/requests`
4. ✅ Добавляет WebSocket уведомления в approve/deny/return

---

## 🎉 ГОТОВО!

После деплоя авторизация будет сохраняться при перезагрузке! 🚀
