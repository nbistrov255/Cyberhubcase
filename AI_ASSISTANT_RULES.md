# 🤖 ПРАВИЛА ДЛЯ AI АССИСТЕНТА

## 📌 Проект: CyberHub - Case Opening Platform

---

## 🔒 КРИТИЧЕСКИЕ ФАЙЛЫ - НЕ ИЗМЕНЯТЬ БЕЗ ЯВНОГО ЗАПРОСА!

### 1. `/vite.config.ts`

**НИКОГДА не удаляйте:**
- ✅ Плагин `tailwindcss()` - обязателен для Tailwind v4
- ✅ Секцию `server.proxy` - обязательна для работы с API

**Эталонная конфигурация:**
```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'
import path from "path"

export default defineConfig({
  plugins: [
    react(),
    tailwindcss(), // Required for Tailwind v4
  ],
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "./src"),
    },
  },
  server: {
    proxy: {
      '/api': {
        target: 'http://91.107.120.48:3000',
        changeOrigin: true,
        secure: false,
      }
    }
  }
})
```

**См. подробно:** [VITE_CONFIG_REFERENCE.md](./VITE_CONFIG_REFERENCE.md)

---

### 2. `/src/styles/tailwind.css`

**Правильный синтаксис для Tailwind v4:**
```css
@import 'tailwindcss';
@import 'tw-animate-css';
```

**❌ НЕ использовать синтаксис v3:**
```css
/* НЕПРАВИЛЬНО: */
@tailwind base;
@tailwind components;
@tailwind utilities;
```

---

### 3. `/src/main.tsx`

**CSS ВСЕГДА должен быть ПЕРВЫМ импортом:**
```tsx
import './styles/index.css';  // ← ПЕРВАЯ СТРОКА!
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import App from './app/App';

// ...
```

---

### 4. `/postcss.config.mjs`

**Должен быть ПУСТЫМ для Tailwind v4:**
```javascript
export default {}
```

**❌ НЕ добавлять плагины:**
```javascript
// НЕПРАВИЛЬНО для v4:
export default {
  plugins: {
    tailwindcss: {},     // ← НЕ НУЖНО!
    autoprefixer: {},    // ← НЕ НУЖНО!
  },
}
```

---

### 5. ❌ НЕ СОЗДАВАТЬ: `tailwind.config.ts`

**Этот файл НЕ должен существовать!**

Проект использует Tailwind CSS v4, который работает без этого файла.

---

## 🎯 СТАНДАРТЫ ПРОЕКТА

### Версии технологий:

| Технология | Версия | Важность |
|------------|--------|----------|
| React | 18.3.1 | Критично |
| TypeScript | 5.7.2 | Критично |
| Vite | 6.3.5 | Критично |
| Tailwind CSS | **4.1.12** | ⚠️ **v4, НЕ v3!** |
| @tailwindcss/vite | 4.1.12 | Критично |

### API Backend:
- **URL:** `http://91.107.120.48:3000`
- **Proxy:** `/api` → `http://91.107.120.48:3000/api`
- **Авторизация:** `Authorization: Bearer <token>` (кроме логина)

### Цветовая схема:
- **Фон:** `#17171c` (темно-серый, почти черный)
- **Акцент:** `#7c2d3a` (бордовый)
- **Карточки:** `#1d1d22`
- **Границы:** `rgba(255, 255, 255, 0.1)`

---

## 📝 ПРАВИЛА РАЗРАБОТКИ

### При создании/изменении файлов:

#### 1. API запросы
**ВСЕГДА используйте прокси:**
```typescript
// ✅ ПРАВИЛЬНО:
fetch('/api/inventory')

// ❌ НЕПРАВИЛЬНО:
fetch('http://91.107.120.48:3000/api/inventory')
```

#### 2. Импорт компонентов
**Используйте alias `@`:**
```typescript
// ✅ ПРЕДПОЧТИТЕЛЬНО:
import { Component } from '@/components/Component'

// ⚠️ Можно, но не рекомендуется:
import { Component } from '../../components/Component'
```

#### 3. Tailwind классы
**НЕ используйте классы для типографики без запроса:**
```tsx
// ❌ НЕ ДЕЛАЙТЕ:
<h1 className="text-4xl font-bold leading-tight">

// ✅ ПРАВИЛЬНО:
<h1 className="uppercase tracking-wider">
```

**Причина:** В `/src/styles/theme.css` настроена типографика для каждого HTML элемента.

#### 4. Изображения
**Используйте компонент ImageWithFallback:**
```tsx
import { ImageWithFallback } from '@/components/figma/ImageWithFallback'

// Для новых изображений:
<ImageWithFallback src={url} alt="Description" />

// НЕ используйте обычный <img>:
// <img src={url} alt="Description" />  ← НЕПРАВИЛЬНО
```

---

## 🔄 WORKFLOW ПРИ ИЗМЕНЕНИЯХ

### Изменение конфигурационных файлов:

```bash
# 1. Остановите сервер
Ctrl+C

# 2. Очистите кэш Vite
rm -rf node_modules/.vite dist

# 3. Запустите заново
npm run dev
```

### Добавление новых npm пакетов:

```bash
# 1. Установите пакет
npm install package-name

# 2. Очистите кэш (опционально)
rm -rf node_modules/.vite

# 3. Перезапустите
npm run dev
```

### Изменение CSS файлов:

Vite автоматически перезагрузит CSS без перезапуска сервера.

**НО:** Если изменяете `@import` директивы - очистите кэш!

---

## ⚠️ ЧАСТЫЕ ОШИБКИ

### ❌ Ошибка 1: Удаление tailwindcss() из vite.config.ts

**Симптомы:**
- Белый экран
- Стили не применяются
- В консоли: "Unknown at rule @import"

**Решение:**
Верните плагин в vite.config.ts (см. эталонную конфигурацию).

---

### ❌ Ошибка 2: Создание tailwind.config.ts

**Симптомы:**
- Конфликт конфигураций
- Стили компилируются неправильно
- Кастомные цвета не работают

**Решение:**
Удалите файл! Tailwind v4 его не использует.

---

### ❌ Ошибка 3: Использование hardcoded API URLs

**Симптомы:**
- CORS ошибки
- Запросы не проходят через прокси
- В production не работает

**Решение:**
Замените на `/api/...` (использование прокси).

---

### ❌ Ошибка 4: CSS импорт не первый в main.tsx

**Симптомы:**
- Стили применяются с задержкой
- FOUC (Flash of Unstyled Content)
- Неправильный порядок каскада CSS

**Решение:**
Переместите `import './styles/index.css'` на первую строку.

---

## 📚 ДОКУМЕНТАЦИЯ ПРОЕКТА

### Основные файлы:

1. **[README.md](./README.md)** - Общая информация о проекте
2. **[VITE_CONFIG_REFERENCE.md](./VITE_CONFIG_REFERENCE.md)** - Эталонная конфигурация Vite
3. **[TAILWIND_V3_VS_V4.md](./TAILWIND_V3_VS_V4.md)** - Различия версий Tailwind
4. **[ПОШАГОВАЯ_ИНСТРУКЦИЯ.md](./ПОШАГОВАЯ_ИНСТРУКЦИЯ.md)** - Решение проблем со стилями

### Для backend разработчиков:

1. **[FRONTEND_CHANGELOG.md](./FRONTEND_CHANGELOG.md)** - Все API endpoints
2. **[API_EXAMPLES.md](./API_EXAMPLES.md)** - Примеры запросов
3. **[INTEGRATION_TESTS.md](./INTEGRATION_TESTS.md)** - Тестирование

### Для администраторов:

1. **[ADMIN_README.md](./ADMIN_README.md)** - Админ-панель
2. **[DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md)** - Деплой

---

## 🎯 ПРИОРИТЕТЫ ПРИ ОТВЕТАХ

### 1. Безопасность конфигурации
- ✅ Проверяйте, не нарушает ли запрос эталонную конфигурацию
- ✅ Предупреждайте, если запрос может сломать проект

### 2. Соответствие стандартам проекта
- ✅ Tailwind v4 синтаксис
- ✅ Использование прокси для API
- ✅ Правильные импорты

### 3. Ясность и подробность
- ✅ Объясняйте "почему", а не только "как"
- ✅ Предоставляйте примеры кода
- ✅ Указывайте на потенциальные проблемы

---

## 🧠 КОНТЕКСТ ПРОЕКТА

### Тип проекта:
Desktop-style приложение для открытия кейсов (как CS:GO cases)

### Компоненты:
1. **Client App** - Пользовательская часть (открытие кейсов, инвентарь)
2. **Admin Panel** - Панель управления (айтемы, кейсы, пользователи)

### Особенности:
- Мультиязычность (EN, RU, LV)
- Темная игровая тема (#17171c)
- Бордовый акцент (#7c2d3a)
- Анимации открытия кейсов (рулетка)
- Инвентарь с продажей предметов
- Живая лента дропов

---

## ✅ ПЕРЕД КАЖДЫМ ОТВЕТОМ ПРОВЕРЯЙ:

- [ ] Не нарушает ли предложение эталонную конфигурацию vite.config.ts?
- [ ] Используется ли правильный синтаксис Tailwind v4?
- [ ] Используются ли API запросы через прокси (`/api/...`)?
- [ ] CSS импорт первый в файле?
- [ ] Не предлагается ли создать `tailwind.config.ts`?
- [ ] Соответствует ли код стилю проекта?

---

## 🆘 ПРИ ВОЗНИКНОВЕНИИ ПРОБЛЕМ

### Если пользователь сообщает о белом экране:

1. ✅ Проверить `vite.config.ts` - наличие `tailwindcss()` плагина
2. ✅ Проверить `src/main.tsx` - CSS импорт первым
3. ✅ Проверить `src/styles/tailwind.css` - синтаксис v4
4. ✅ Предложить очистить кэш: `rm -rf node_modules/.vite dist`
5. ✅ НЕ предлагать создать `tailwind.config.ts`!

### Если пользователь сообщает о проблемах с API:

1. ✅ Проверить `vite.config.ts` - наличие `server.proxy`
2. ✅ Проверить запросы - используется ли `/api/...`
3. ✅ Проверить backend - работает ли `http://91.107.120.48:3000`
4. ✅ Проверить DevTools → Network - видны ли запросы

---

## 📞 ЭСКАЛАЦИЯ

Если проблему нельзя решить стандартными методами:

1. Попросить пользователя выполнить диагностику: `bash diagnose.sh`
2. Попросить скриншоты DevTools (Console, Network)
3. Попросить вывод команды `npm run dev`
4. Попросить содержимое критичных файлов

---

**Последнее обновление:** 28 декабря 2024  
**Статус:** ✅ Эталонные правила зафиксированы  
**Важность:** 🔴 КРИТИЧНО - соблюдать при всех изменениях
