# 🧪 Testing Instructions - Money Claim Fix

## 📦 Деплой обновлённого backend

```bash
# 1. Скопировать файл на VDS
scp backend_reference/index.ts root@91.107.120.48:/app/src/index.ts

# 2. Перезапустить контейнер
ssh root@91.107.120.48
cd /path/to/docker/
docker-compose restart cyberhub_api

# 3. Проверить логи
docker-compose logs -f cyberhub_api
```

**Ожидаемый результат:**
```
[nodemon] starting `ts-node src/index.ts`
[Backend] Started on port 3000
```

❌ **НЕ должно быть:**
```
TSError: ⨯ Unable to compile TypeScript
This operation was aborted (сразу после запуска)
```

---

## 🔍 Тестирование функционала "ПОЛУЧИТЬ ДЕНЬГИ"

### Шаг 1: Открой кейс с money item

1. Зайди в приложение (http://91.107.120.48 или твой домен)
2. Войди под клиентом `+37127177620`
3. Открой любой кейс (например Daily)
4. Дождись пока выпадет предмет типа "money" (5€, 10€ и т.д.)
5. Перейди в **Inventory** (страница с полученными предметами)

---

### Шаг 2: Проверь логи ПЕРЕД нажатием "ПОЛУЧИТЬ"

```bash
docker-compose logs -f cyberhub_api
```

Должны быть только обычные запросы:
```
📡 GET /api/profile
📡 GET /api/inventory
📡 GET /api/drops/recent
```

---

### Шаг 3: Нажми "ПОЛУЧИТЬ" на money item

**ВАЖНО:** Нажми **ОДИН РАЗ** и жди!

---

### Шаг 4: Проверь логи ПОСЛЕ нажатия

**✅ ПРАВИЛЬНЫЕ логи (всё работает):**

```
📡 POST /api/inventory/claim
📥 CLAIM REQUEST RECEIVED
👤 User: 5704047b-b551-4330-bb71-0219fd96dd89, Item: 4
💰 Auto-claiming money...
🔒 Item locked (status = 'processing')
💰 [SmartShell] Adding 5€ BONUS to 5704047b-b551-4330-bb71-0219fd96dd89
🔑 Service token obtained
📡 Step 1/2: Fetching current BONUS balance...
✅ Step 1/2: Received 2543 clients
📊 Current BONUS: 100€, Adding: 5€, New: 105€
📡 Step 2/2: Setting new BONUS balance...
✅ BONUS updated: 105€ (added 5€)
✅ Money added
📡 GET /api/profile
```

**❌ НЕПРАВИЛЬНЫЕ логи (есть проблема):**

```
Fetch Error: This operation was aborted
❌ Failed to add BONUS: This operation was aborted
❌ Failed to add balance, item restored
```

---

### Шаг 5: Проверь Frontend поведение

**✅ Правильное поведение:**

1. **Карточка исчезла** из списка инвентаря (без перезагрузки страницы)
2. **Баланс в TopBar обновился** автоматически (+5€ или сколько было на карточке)
3. **Страница НЕ перезагрузилась** (остались на Inventory)
4. **Console в DevTools** показывает:
   ```
   ✅ Money claimed successfully
   ✅ Profile refreshed successfully
   ```

**❌ Неправильное поведение:**

- Карточка **НЕ исчезла** (осталась в списке)
- Баланс **НЕ обновился**
- Появилась **ошибка** в Console (красная)
- Страница **перезагрузилась**

---

### Шаг 6: Проверь SmartShell админку

1. Зайди в SmartShell админку: https://admin.smartshell.gg
2. Найди клиента `+37127177620` (или по UUID `5704047b-b551-4330-bb71-0219fd96dd89`)
3. Открой вкладку **"Бонусы"** или **"Баланс"**
4. **Проверь БОНУСНЫЙ баланс** - он должен увеличиться на сумму из карточки

**Пример:**
- Было: 100€ (bonus)
- Нажал "ПОЛУЧИТЬ" 5€
- Стало: 105€ (bonus)

**ВАЖНО:** Проверяй именно **БОНУСНЫЙ** баланс, не основной deposit!

---

## 🐛 Troubleshooting

### Проблема 1: "This operation was aborted" всё ещё появляется

**Причина:** SmartShell API очень медленный (даже 90 сек мало)

**Решение:**

1. Проверь скорость сети до SmartShell API:
   ```bash
   curl -o /dev/null -s -w "Total time: %{time_total}s\n" https://billing.smartshell.gg/api/graphql
   ```

2. Если > 30 секунд, увеличь timeout до 120 сек:
   ```typescript
   // index.ts:61
   const timeoutId = setTimeout(() => controller.abort(), 120000); // 120 сек
   ```

---

### Проблема 2: "Client not found" в логах

**Причина:** UUID клиента не найден в списке

**Проверь:**

1. UUID правильный:
   ```bash
   docker-compose logs cyberhub_api | grep "User:"
   # Должно быть: 👤 User: 5704047b-b551-4330-bb71-0219fd96dd89
   ```

2. Клиент существует в SmartShell:
   ```bash
   curl -X POST https://billing.smartshell.gg/api/graphql \
     -H "Authorization: Bearer YOUR_SERVICE_TOKEN" \
     -H "Content-Type: application/json" \
     -d '{"query":"query { clients(page: 1, first: 10) { data { uuid login } } }"}'
   ```

---

### Проблема 3: Баланс не пополнился в SmartShell

**Причина:** setBonus mutation вернула ошибку (но её не видно в логах)

**Проверь:**

1. Права сервисного аккаунта:
   - Зайди в SmartShell админку
   - Проверь, что сервисный аккаунт имеет права на `setBonus` mutation

2. Тест setBonus вручную:
   ```bash
   curl -X POST https://billing.smartshell.gg/api/graphql \
     -H "Authorization: Bearer YOUR_SERVICE_TOKEN" \
     -H "Content-Type: application/json" \
     -d '{
       "query": "mutation { setBonus(input: { client_uuid: \"5704047b-...\", value: 105 }) { uuid login } }"
     }'
   ```

---

### Проблема 4: Карточка не исчезает из инвентаря

**Причина:** Frontend не обновляет список после claim

**Проверь:**

1. Response от API:
   - Открой DevTools → Network → XHR
   - Найди запрос `/api/inventory/claim`
   - Проверь Response:
     ```json
     {
       "success": true,
       "type": "money",
       "message": "Added 5€ to balance"
     }
     ```

2. Если `success: false` → смотри backend логи

3. Если `success: true`, но карточка не исчезла:
   - Проверь Console в DevTools
   - Должно быть: `✅ Profile refreshed successfully`
   - Если нет → проблема в `refreshProfile()` функции

---

## 🔒 Тест защиты от дублирования

### Сценарий: Быстро нажать "ПОЛУЧИТЬ" 3 раза подряд

**Шаг 1:** Открой кейс, получи money item  
**Шаг 2:** Быстро нажми "ПОЛУЧИТЬ" **3 раза** (click-click-click!)  
**Шаг 3:** Проверь логи

**✅ Правильное поведение:**

```
📡 POST /api/inventory/claim  # 1-й запрос
📥 CLAIM REQUEST RECEIVED
🔒 Item locked (status = 'processing')
💰 [SmartShell] Adding 5€ BONUS...

📡 POST /api/inventory/claim  # 2-й запрос
📥 CLAIM REQUEST RECEIVED
❌ Item status is processing, expected 'available'

📡 POST /api/inventory/claim  # 3-й запрос
📥 CLAIM REQUEST RECEIVED
❌ Item status is processing, expected 'available'

✅ BONUS updated: 105€ (added 5€)  # Только 1 раз!
```

**Результат:**
- Баланс пополнился **ОДИН РАЗ** (+5€)
- Item помечен как `status='received'`
- Повторные нажатия **отклонены**

**❌ Неправильное поведение:**

```
✅ BONUS updated: 105€
✅ BONUS updated: 110€  # ❌ ДУБЛЬ!
✅ BONUS updated: 115€  # ❌ ДУБЛЬ!
```

Если такое произошло → защита НЕ работает!

---

## 📊 Финальный чек-лист

После успешного деплоя и тестирования:

- [ ] Backend компилируется без ошибок TypeScript
- [ ] Timeout 90 секунд достаточен (нет "This operation was aborted")
- [ ] При нажатии "ПОЛУЧИТЬ" логи показывают полный процесс (Step 1/2, Step 2/2)
- [ ] Карточка исчезает из инвентаря БЕЗ перезагрузки страницы
- [ ] Баланс в TopBar обновляется автоматически
- [ ] БОНУСНЫЙ баланс в SmartShell админке увеличился
- [ ] Защита от дублирования работает (невозможно нажать дважды)
- [ ] UUID клиента правильный (`5704047b-b551-4330-bb71-0219fd96dd89`)

---

## 🎉 Если всё работает

**Поздравляю!** Функционал "ПОЛУЧИТЬ ДЕНЬГИ" работает корректно:

✅ Реальное пополнение БОНУСОВ через SmartShell API  
✅ Timeout 90 сек достаточен для тяжёлых запросов  
✅ Защита от дублирования работает  
✅ Frontend обновляется без перезагрузки  
✅ Детальное логирование для отладки  

---

## 📞 Если проблемы остались

Дай мне:

1. **Полные логи** из VDS:
   ```bash
   docker-compose logs cyberhub_api > logs.txt
   ```

2. **Network logs** из DevTools (XHR запросы):
   - Request URL
   - Request Headers
   - Response Body

3. **Console logs** из DevTools (F12 → Console)

4. **SmartShell баланс** клиента ПЕРЕД и ПОСЛЕ нажатия "ПОЛУЧИТЬ"

И я помогу найти проблему! 🚀
