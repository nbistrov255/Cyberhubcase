# AI Assistant Workflow Rules

**Это инструкция для AI ассистента (меня) по работе с Backend Reference.**

---

## 🎯 ОБЯЗАТЕЛЬНЫЙ АЛГОРИТМ

### ПРИ ЛЮБОМ ЗАПРОСЕ ПОЛЬЗОВАТЕЛЯ:

#### ШАГ 1: АНАЛИЗ BACKEND
```
1. Открой `/backend_reference/index.ts`
2. Найди нужный endpoint (или убедись что его НЕТ)
3. Проверь:
   - Какие параметры принимает
   - Какую структуру возвращает
   - Какие заголовки требуются
   - Какие ошибки может вернуть
```

#### ШАГ 2: ЕСЛИ ENDPOINT НЕ НАЙДЕН
```
1. Спроектируй новый endpoint в `/backend_reference/index.ts`
2. Если нужна новая таблица - обнови `/backend_reference/database.ts`
3. Обнови `/backend_reference/API_REFERENCE.md`
4. Обнови `/backend_reference/CHANGELOG.md`
5. Добавь endpoint в `/src/config/api.ts`
6. Только ПОТОМ пиши Frontend код
```

#### ШАГ 3: ПИШИ FRONTEND КОД
```
1. Используй ТОЧНЫЕ названия полей из backend
2. Используй правильную структуру response
3. Добавь обработку всех возможных ошибок
4. Используй getAuthHeaders() для protected endpoints
```

---

## ⚠️ КРИТИЧЕСКИЕ ПРАВИЛА

### 🚫 ЗАПРЕЩЕНО

1. **НЕ придумывай** структуру данных - всегда смотри backend
2. **НЕ меняй** названия полей без синхронизации с backend
3. **НЕ трогай** SmartShell функции:
   - `gqlRequest()`
   - `getServiceToken()`
   - `getClientBalance()`
   - `calculateProgressSafe()`
4. **НЕ добавляй** новые библиотеки без согласования
5. **НЕ пиши** Frontend код БЕЗ проверки backend

### ✅ ОБЯЗАТЕЛЬНО

1. **ВСЕГДА** читай `/backend_reference/index.ts` перед кодом
2. **ВСЕГДА** обновляй документацию при изменениях
3. **ВСЕГДА** проверяй типы данных (TypeScript)
4. **ВСЕГДА** добавляй обработку ошибок
5. **ВСЕГДА** используй `getAuthHeaders()` для защищенных endpoints

---

## 📋 ЧЕКЛИСТ ДЛЯ КАЖДОЙ ЗАДАЧИ

```
[ ] Прочитал запрос пользователя
[ ] Открыл /backend_reference/index.ts
[ ] Нашел нужный endpoint (или убедился что его нет)
[ ] Проверил структуру request/response
[ ] Если endpoint отсутствует:
    [ ] Спроектировал новый endpoint
    [ ] Обновил database.ts (если нужна таблица)
    [ ] Обновил API_REFERENCE.md
    [ ] Обновил CHANGELOG.md
    [ ] Добавил в /src/config/api.ts
[ ] Написал Frontend код:
    [ ] Правильные названия полей
    [ ] Обработка ошибок
    [ ] TypeScript типы
    [ ] Auth headers (если нужно)
[ ] Протестировал логику (мысленно)
[ ] Ответил пользователю
```

---

## 🔍 ПРИМЕРЫ ПРАВИЛЬНОЙ РАБОТЫ

### Пример 1: Пользователь просит "Добавь Live Feed"

**МОЙ ПРОЦЕСС:**
```
1. Открываю /backend_reference/index.ts
2. Ищу "drops" или "feed"
3. Нашел: GET /api/drops/recent
4. Проверяю response:
   {
     success: true,
     drops: [{
       id, item_name, image, rarity, timestamp, user_uuid, user_name
     }]
   }
5. Иду в /src/app/components/TopBar.tsx
6. Пишу код с ТОЧНЫМИ полями: item_name (НЕ prize_name!)
7. Добавляю useEffect с интервалом 10s
8. Готово!
```

### Пример 2: Пользователь просит "Добавь топ игроков"

**МОЙ ПРОЦЕСС:**
```
1. Открываю /backend_reference/index.ts
2. Ищу "leaderboard" или "top"
3. НЕ НАШЕЛ!
4. Проектирую новый endpoint:

   app.get("/api/leaderboard", async (req, res) => {
     const top = await db.all(`
       SELECT user_uuid, nickname, xp, level 
       FROM user_settings 
       ORDER BY xp DESC 
       LIMIT 10
     `);
     res.json({ success: true, players: top });
   });

5. Сохраняю в /backend_reference/index.ts
6. Обновляю API_REFERENCE.md
7. Обновляю CHANGELOG.md (Added: GET /api/leaderboard)
8. Добавляю в /src/config/api.ts:
   getLeaderboard: `${API_BASE}/api/leaderboard`
9. Создаю компонент LeaderboardPage.tsx
10. Использую правильную структуру: data.players
11. Готово!
```

### Пример 3: Пользователь просит "Исправь баг в claim"

**МОЙ ПРОЦЕСС:**
```
1. Открываю /backend_reference/index.ts
2. Нахожу app.post("/api/inventory/claim", ...)
3. Читаю всю логику
4. Проверяю:
   - Проверка trade_link для скинов ✓
   - Автовывод для money ✓
   - Создание request для skin/physical ✓
5. Иду в /src/app/components/InventoryPage.tsx
6. Проверяю соответствие:
   - Отправляем inventory_id ✓
   - Проверяем error === 'TRADE_LINK_MISSING' ✓
   - Обрабатываем type === 'money' и type === 'item' ✓
7. Нахожу баг и исправляю
8. Готово!
```

---

## 🧠 МЕНТАЛЬНАЯ МОДЕЛЬ

**ДУМАЙ ТАК:**

```
Backend = Источник истины
Frontend = Отображение данных из Backend

Если Backend возвращает:
{
  "drops": [{ "item_name": "AK-47" }]
}

То Frontend ОБЯЗАН использовать:
data.drops[0].item_name

НЕ data.spins[0].prize_name!
```

---

## 📚 ФАЙЛЫ ДЛЯ СПРАВКИ

### Быстрая справка
- `/backend_reference/QUICK_REFERENCE.md` - самое важное

### Полная документация
- `/backend_reference/API_REFERENCE.md` - все endpoints

### Код
- `/backend_reference/index.ts` - backend логика
- `/backend_reference/database.ts` - схема БД

### Статус
- `/backend_reference/INTEGRATION_STATUS.md` - что работает
- `/backend_reference/CHANGELOG.md` - история изменений

---

## 🎬 WORKFLOW В ДЕЙСТВИИ

### Когда пользователь пишет запрос:

1️⃣ **STOP** - не пиши код сразу!

2️⃣ **READ** `/backend_reference/index.ts` или `QUICK_REFERENCE.md`

3️⃣ **THINK** - есть ли endpoint? какая структура данных?

4️⃣ **DESIGN** - если нет endpoint, спроектируй его

5️⃣ **CODE** - напиши Frontend код с правильной интеграцией

6️⃣ **DOCUMENT** - обнови документацию

7️⃣ **RESPOND** - ответь пользователю

---

## 💡 ПОЛЕЗНЫЕ ШПАРГАЛКИ

### Частые ошибки и как их избежать:

❌ **Ошибка:** `data.spins is undefined`
✅ **Правильно:** Проверь backend - он возвращает `data.drops`

❌ **Ошибка:** `401 Unauthorized`
✅ **Правильно:** Добавь `Authorization: Bearer <token>` через `getAuthHeaders()`

❌ **Ошибка:** `item.prize_name is undefined`
✅ **Правильно:** Backend возвращает `item_name`, не `prize_name`

❌ **Ошибка:** `TRADE_LINK_MISSING` не обрабатывается
✅ **Правильно:** Добавь проверку и покажи модалку

---

## 🔄 СИНХРОНИЗАЦИЯ ИЗМЕНЕНИЙ

### Если меняешь поле в Backend:

```typescript
// БЫЛО:
prize_name

// СТАЛО:
item_title
```

**ТО ОБЯЗАН ОБНОВИТЬ:**
1. `/backend_reference/index.ts` - код backend
2. `/backend_reference/API_REFERENCE.md` - документация
3. `/backend_reference/CHANGELOG.md` - запись изменения
4. `/src/config/api.ts` - если нужно
5. **ВСЕ** Frontend компоненты где используется это поле:
   - TopBar.tsx
   - InventoryPage.tsx
   - RequestsPage.tsx
   - и т.д.

---

## 🎓 ОБУЧЕНИЕ НА ПРИМЕРАХ

### ДО: Плохой код (без проверки backend)
```typescript
// Пользователь: "Покажи недавние дропы"
const fetchDrops = async () => {
  const res = await fetch('/api/recent');
  const data = await res.json();
  setItems(data.items); // ОШИБКА! Не проверил backend
};
```

### ПОСЛЕ: Хороший код (с проверкой backend)
```typescript
// 1. Открыл /backend_reference/index.ts
// 2. Нашел: GET /api/drops/recent
// 3. Response: { success: true, drops: [...] }
// 4. Пишу правильный код:

const fetchDrops = async () => {
  const res = await fetch(`${API_BASE}/api/drops/recent`);
  const data = await res.json();
  
  if (data.success && data.drops) {
    setItems(data.drops.map(drop => ({
      id: drop.id,
      name: drop.item_name,  // ← Правильное поле!
      image: drop.image,
      user: drop.user_name
    })));
  }
};
```

---

## 🏆 КАЧЕСТВО КОДА

### ЧТО ДЕЛАЕТ КОД ХОРОШИМ:

✅ Соответствует структуре данных из backend  
✅ Обрабатывает все возможные ошибки  
✅ Использует TypeScript типы  
✅ Документирован (комментарии для сложной логики)  
✅ Использует helper функции (`getAuthHeaders()`)  

### ЧТО ДЕЛАЕТ КОД ПЛОХИМ:

❌ Придуманные названия полей  
❌ Отсутствие обработки ошибок  
❌ Использование `any` вместо типов  
❌ Дублирование кода  
❌ Захардкоженные значения  

---

## 🎯 ИТОГОВАЯ МАНТРА

```
1. READ backend first
2. UNDERSTAND data structure
3. DESIGN if missing
4. CODE with precision
5. DOCUMENT changes
```

**Эти 5 шагов - закон!**

---

**Version:** 1.0  
**Last Updated:** 2024-12-30  
**Created for:** Claude AI Assistant
