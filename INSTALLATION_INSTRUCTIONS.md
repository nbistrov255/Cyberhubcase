# 📦 Инструкция по установке WebSocket на VDS

## ✅ Что уже готово:

1. ✅ **Frontend** - WebSocket клиент полностью настроен
2. ✅ **Backend** - Готовый код в папке `/backend`
3. ✅ **package.json** - Socket.io уже добавлен
4. ✅ **Индикатор подключения** - В TopBar (Wi-Fi иконка)

---

## 🚀 Что тебе нужно сделать:

### Шаг 1: Скачать файлы

Скачай эти файлы из проекта:

```
/backend/src/index.ts       → Замени свой backend/src/index.ts
/backend/package.json       → Замени свой backend/package.json
/backend/.env.example       → Создай .env на основе этого файла
/backend/README.md          → Для справки
```

### Шаг 2: Загрузить на VDS

Загрузи эти файлы на свой VDS в папку backend:

```bash
# На VDS выполни:
cd /path/to/cyberhub/backend

# Замени файлы (через FTP, SFTP или любым удобным способом)
```

### Шаг 3: Пересобрать Docker

После загрузки файлов, выполни на VDS:

```bash
# Останови контейнеры
docker-compose down

# Пересобери с новым кодом
docker-compose up --build -d

# Проверь логи
docker-compose logs -f backend
```

### Шаг 4: Проверка работы

**1. Проверь что сервер запустился:**

В логах должно быть:
```
✅ Server with WebSocket running on port 3000
🟢 Client connected: <socket-id>
```

**2. Проверь через curl:**

```bash
curl http://91.107.120.48:3000/health
```

Ответ должен быть:
```json
{
  "status": "ok",
  "websocket": "active",
  "clients": 0
}
```

**3. Проверь во frontend:**

Открой сайт → DevTools → Console → должно быть:
```
🔌 Initializing WebSocket connection...
✅ WebSocket connected: <socket-id>
```

**4. Проверь индикатор:**

В TopBar справа должна быть **🟢 зеленая Wi-Fi иконка**.

---

## 🔥 Как использовать WebSocket

### Когда админ изменяет кейсы:

После любого изменения кейса (добавление/удаление/обновление) backend автоматически отправит событие `cases:updated`, и список кейсов на сайте обновится **мгновенно без перезагрузки страницы**.

### Когда пользователь открывает кейс:

После открытия кейса баланс пользователя обновится **автоматически** через WebSocket.

### Когда пользователь пополняет баланс:

После успешного платежа баланс обновится **мгновенно** без перезагрузки.

---

## 📊 Мониторинг

### Проверить количество подключений:

```bash
curl http://91.107.120.48:3000/health
```

Параметр `clients` покажет сколько пользователей онлайн.

### Логи в реальном времени:

```bash
docker-compose logs -f backend
```

Там будешь видеть:
- Подключения пользователей
- События WebSocket
- Ошибки (если есть)

---

## ⚠️ Если что-то не работает:

### 1. WebSocket не подключается

**Проверь порт 3000:**
```bash
sudo ufw allow 3000
```

**Проверь что сервер запущен:**
```bash
docker ps
```

**Проверь логи:**
```bash
docker-compose logs backend
```

### 2. Индикатор красный в TopBar

Это значит frontend не может подключиться к WebSocket.

**Проверь:**
- Сервер запущен: `docker ps`
- Порт открыт: `telnet 91.107.120.48 3000`
- Firewall настроен: `sudo ufw status`

### 3. События не приходят

**Проверь в логах backend:**
```bash
docker-compose logs -f backend
```

Должны видеть:
```
🔥 WebSocket: cases:updated emitted
💰 WebSocket: balance updated for user <id>
```

Если этого нет - значит событие не отправляется. Проверь что код правильно вызывает:
```typescript
io.emit('cases:updated');
```

---

## 🎯 Частые вопросы:

**Q: Нужно ли менять frontend код?**
A: Нет! Frontend уже полностью готов, ничего менять не нужно.

**Q: Нужно ли устанавливать socket.io через npm?**
A: Нет! При пересборке Docker сам установит все зависимости из package.json.

**Q: Как узнать что WebSocket работает?**
A: Смотри на индикатор в TopBar - если зеленый (Wi-Fi) = работает, если красный (WifiOff) = не работает.

**Q: Влияет ли WebSocket на существующий API?**
A: Нет! WebSocket работает параллельно, ничего не ломает.

**Q: Что делать если интернет пропал?**
A: Frontend автоматически переподключится когда интернет вернется.

---

## 🎉 Готово!

После выполнения всех шагов:

✅ WebSocket сервер работает  
✅ Frontend автоматически подключается  
✅ Индикатор показывает статус подключения  
✅ Real-time обновления работают  

**Теперь при любых изменениях кейсов через админку - все пользователи увидят обновления мгновенно без перезагрузки страницы!** 🚀

---

## 📞 Нужна помощь?

Если что-то не работает:

1. Проверь логи: `docker-compose logs -f backend`
2. Проверь health: `curl http://91.107.120.48:3000/health`
3. Проверь индикатор в TopBar
4. Напиши мне - помогу разобраться!

---

**Всё! Теперь просто скачай файлы из `/backend` и загрузи на VDS!** 📦
